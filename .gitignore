# OneDrive / Windows が撒くもの
desktop.ini
Thumbs.db
ehthumbs.db
$RECYCLE.BIN/

# Office の一時ファイル
~$*

# Claude Code の PC 固有設定（共有しない）
.claude/settings.local.json

# macOS（他の PC が Mac の場合に備えて）
.DS_Store

# 注意: inbox/ は無視しない。
# 生ログだが「いつ何を見たか」の記録として履歴に価値があるため追跡する。
# notes/ digests/ state/ related-work/ も同様にすべて追跡対象。
