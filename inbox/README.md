# inbox — 収集の生ログ

`/collect` が `YYYY-MM-DD.md` という名前で、その日の新着だけを書き出す。

**使い捨ててよい。** `/digest` で月次ダイジェストに畳んだ後は、古い月のファイルを消して構わない。既読判定は `state/seen.jsonl` が持っているので、ここを消しても同じものが再び新着として出ることはない。

ここは「速く・雑に」の層。精読して残すものは `notes/` に移る。
